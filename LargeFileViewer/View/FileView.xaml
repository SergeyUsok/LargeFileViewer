<UserControl x:Class="LargeFileViewer.View.FileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary Source="Resources/MainWindowResources.xaml"/>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="33*"/>
            <RowDefinition Height="413*"/>
        </Grid.RowDefinitions>

        
        <TextBlock Text="{Binding RowsCount, StringFormat={}Rows count: {0:n0}}" Grid.Row="0" Margin="14,0,0,0" FontWeight="Bold" HorizontalAlignment="Left"/>

        <DataGrid ItemsSource="{Binding FileRowsCollection}" Style="{StaticResource FileView}" Grid.Row="1" />
    </Grid>

</UserControl>
